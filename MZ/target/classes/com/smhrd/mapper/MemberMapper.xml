<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.mapper.MemberMapper">
	
	<insert id="join" parameterType="com.smhrd.entity.Member">
		insert into member values(#{email}, #{pw}, #{tel}, #{address})
	</insert>
	
	<select id="login" parameterType="com.smhrd.entity.Member" resultType="com.smhrd.entity.Member">
		select * from member where email=#{email} and pw=#{pw}
	</select>
	
	<update id="update" parameterType="com.smhrd.entity.Member">
		update member set pw=#{pw}, tel=#{tel}, address=#{address} 
		where email=#{email}
	</update>
	
	<select id="showMember" resultType="com.smhrd.entity.Member">
		select * from member where email not in('admin')
	</select>
	
	<delete id="deleteMember">
		delete  from member where email=#{email}
	</delete>
	
	<select id="emailCheck" resultType="com.smhrd.entity.Member">
		select * from member where email=#{email}
	</select>
</mapper>