<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.mapper.BoardMapper">

	<select id="showBoard" resultType="com.smhrd.entity.Board">
		select * from board
	</select>
	
	<delete id="deleteBoard">
		delete from board where idx=#{idx}
	</delete>
	
	<insert id="boardInsert" parameterType="com.smhrd.entity.Board">
		insert into board(title, writer, filename, content) 
		values(#{title}, #{writer}, #{filename}, #{content})
	</insert>
	
	<select id="boardContent" resultType="com.smhrd.entity.Board">
		select * from board where idx=#{idx}
	</select>
</mapper>