<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smhrd.mapper.NotificationMapper">
	
	<insert id="insertNotification" parameterType="com.smhrd.entity.Notification">
		insert into NOTIFICATIONS(user_id, notification_type, entity_idx, message, sender_id)
		values(#{user_id}, #{notification_type}, #{entity_idx}, #{message}, #{sender_id})
	</insert>
	
	<select id="selectNotifyByUserId" resultType="com.smhrd.entity.Notification">
		select * from NOTIFICATIONS where user_id=#{user_id}
	</select>
	
	<update id="updateNotification">
		update NOTIFICATIONS set is_read=#{is_read} where notification_idx=#{notification_idx}
	</update>
	
	<delete id="deleteNotification">
		delete from NOTIFICATIONS where notification_idx=#{notification_idx}
	</delete>
</mapper>